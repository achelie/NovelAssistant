#忽视这个文件，我用来修改文本的地方
用户选择：
  前几章摘要 + 角色(本章要出现的角色) + 世界观(本章需要的世界观) + 角色关系(本章出现的角色关系)
        ↓
Context Builder（核心模块）(要有将选中摘要整合成一个连贯的剧情背景的功能（不超过200字）)
        ↓
结构化 Prompt
        ↓
AI续写 
要有为向量数据库分块，调用智谱api:5c845d4acaa44101a3fca9e75e8e4749.up6deSiCryyv1Mbl(放入env里),只完成后端的部分(用户是通过勾选摘要，角色，世界观，角色关系，然后自己写一个章纲,将这些创建一个表单给后端),将提示词通过rag拼接好后再给智谱api生成本章(尽可能优化token消耗以及生成的质量，质量优先)